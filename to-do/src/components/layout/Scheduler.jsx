import Card from "../UI/Card.jsx";
import SectionTitle from "../UI/SectionTitle.jsx";

export default function Scheduler({ title }) {
  return (
    <>
      <SectionTitle title={title} />
      <Card />
    </>
  );
}
